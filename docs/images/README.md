# Ảnh minh họa cho tài liệu test Frontend

Đặt ảnh chụp màn hình vào thư mục này và dùng đúng tên file gợi ý trong `docs/TESTING-FRONTEND.md`:

- `01-devtools-open.png` – DevTools (F12) đã mở
- `02-network-preserve-log.png` – Tab Network, Preserve log được tick
- `03-network-filter-xhr.png` – Lọc Fetch/XHR, danh sách request API
- `04-request-headers.png` – Request Headers (Authorization, domain, period)
- `05-response-200.png` – Response 200, body có data
- `06-response-401.png` – Response 401/403, body có message
- `07-console-error.png` – Console có lỗi đỏ
- `08-full-check-flow.png` – Trang web + Network khi kiểm tra “không in ra dữ liệu”

Trong file `TESTING-FRONTEND.md`, chèn ảnh bằng cú pháp:

```markdown
![Mô tả](images/01-devtools-open.png)
```
